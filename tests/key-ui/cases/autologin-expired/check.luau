flushPending(4)
print("\n== auto-login with an expired saved key ==")
check("no errors", #ERRORS == 0, ERRORS[1])
check("window stayed open", GENV.VoidHubKeySystem.Destroyed ~= true)
check("expired key removed from disk", FAKE_FS["VoidHub/Key.txt"] == nil, FAKE_FS["VoidHub/Key.txt"])
check("status explains it expired", findByName("Status").Text:find("expired") ~= nil, findByName("Status").Text)
check("button is usable again", findByName("RedeemLabel").Text == "Redeem Key", findByName("RedeemLabel").Text)
check("key left in the box for editing", findByName("KeyInput").Text == "SAVED-KEY-ABC", findByName("KeyInput").Text)
report()
